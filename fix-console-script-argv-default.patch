diff --git a/alexandria/browse.py b/alexandria/browse.py
index adc884c..18da534 100644
--- a/alexandria/browse.py
+++ b/alexandria/browse.py
@@ -5068,7 +5068,10 @@ def _show_db_error_and_quit(app, err):
     dlg.choose(None, None, _on_response)
 
 
-def main(argv):
+def main(argv=None):
+    if argv is None:
+        argv = sys.argv
+
     # Adw.Application initialises libadwaita (theme + dark/light follow
     # the system) and gives us native HeaderBar / Toast support.
     app = Adw.Application(application_id="io.github.pemsley.Alexandria")
diff --git a/alexandria/classify.py b/alexandria/classify.py
index a32ec18..8227b92 100644
--- a/alexandria/classify.py
+++ b/alexandria/classify.py
@@ -72,7 +72,10 @@ def _fmt_rec(rec):
     return "  ".join(bits)
 
 
-def main(argv):
+def main(argv=None):
+    if argv is None:
+        argv = sys.argv
+
     if len(argv) != 2:
         print("usage: {} <directory>".format(argv[0]), file=sys.stderr)
         return 1
diff --git a/alexandria/cli_import.py b/alexandria/cli_import.py
index fd6f538..c5c0157 100644
--- a/alexandria/cli_import.py
+++ b/alexandria/cli_import.py
@@ -18,7 +18,10 @@ import sys
 from . import importer, index
 
 
-def main(argv):
+def main(argv=None):
+    if argv is None:
+        argv = sys.argv
+
     args = argv[1:]
     refresh = False
     if args and args[0] in ("--refresh", "-r"):
